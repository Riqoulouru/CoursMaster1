spring.application.name=td-master-1

# Cette configuration correspond au Docker PostgreSQL fourni aux eleves.
# Si vous changez le .env du Docker, pensez a aligner ces valeurs.
spring.datasource.url=jdbc:postgresql://localhost:5432/coursBasile
spring.datasource.username=user
spring.datasource.password=Pass2026!
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

server.port=8080

# TODO eleves :
# - vous pouvez garder ce socle "Item"
# - ou renommer Item vers votre vrai sujet (Book, Movie, Expense, Task, etc.)
# - l'important est de garder une architecture propre Controller / Service / Repository
